module:
    name: unittests-base
    sources:
        - main.c
    depends:
        - board
    uses:
        - xtimer
subdir:
    - tests-base64
    - tests-bitfield
    - tests-bloom
    - tests-cbor
